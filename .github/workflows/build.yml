name: Build Godot Game (Cross-Platform)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  export:
    name: Export für ${{ matrix.platform }}
    runs-on: ubuntu-latest
    
    container:
      image: barichello/godot-ci:4.6.1 # Deine perfekt angepasste Version!
      
    strategy:
      matrix:
        # Jetzt mit sauberen Namen ohne Leerzeichen und Schrägstriche
        platform: [Windows, macOS, Linux]

    steps:
      - name: 1. Code auschecken
        uses: actions/checkout@v4

      - name: 2. Fehlende Linux-Pakete installieren (Schriftarten-Fix)
        run: apt-get update && apt-get install -y fontconfig

      - name: 3. Export-Ordner vorbereiten
        run: mkdir -p build/${{ matrix.platform }}

      - name: 4. Godot Export ausführen
        run: |
          # WICHTIG: Falls eure project.godot nicht direkt im Hauptverzeichnis des GitHub-Repos liegt,
          # entferne das '#' vor der nächsten Zeile und trage den Ordnernamen deines Spiels ein:
          # cd Name_Eures_Ordners

          # Der Befehl '--path .' sagt Godot genau, wo es suchen soll
          if [ "${{ matrix.platform }}" = "macOS" ]; then
            godot --headless --path . --export-release "${{ matrix.platform }}" build/${{ matrix.platform }}/game.zip
          elif [ "${{ matrix.platform }}" = "Windows" ]; then
            godot --headless --path . --export-release "${{ matrix.platform }}" build/${{ matrix.platform }}/game.exe
          else
            godot --headless --path . --export-release "${{ matrix.platform }}" build/${{ matrix.platform }}/game.x86_64
          fi

      - name: 5. Fertiges Spiel als Download bereitstellen
        uses: actions/upload-artifact@v4
        with:
          name: GodotGame-${{ matrix.platform }}
          path: build/${{ matrix.platform }}/
